<main class="container mx-auto px-4 py-20 text-center">
    <div class="max-w-md mx-auto">
        <div class="text-8xl mb-6">🔍</div>
        <h1 class="text-4xl font-black text-gray-800 mb-4">404</h1>
        <h2 class="text-xl font-bold text-gray-600 mb-4">الصفحة غير موجودة</h2>
        <p class="text-gray-500 mb-8">عذراً، الصفحة التي تبحث عنها غير موجودة أو تم نقلها.</p>
        <div class="flex flex-col sm:flex-row gap-3 justify-center">
            <a href="/<%= typeof countryCode !== 'undefined' ? countryCode : 'sa' %>"
                class="bg-blue-600 text-white font-bold px-8 py-3 rounded-xl hover:bg-blue-700 transition-colors">
                العودة للرئيسية
            </a>
            <a href="/<%= typeof countryCode !== 'undefined' ? countryCode : 'sa' %>/stores"
                class="bg-gray-100 text-gray-700 font-bold px-8 py-3 rounded-xl hover:bg-gray-200 transition-colors">
                تصفح المتاجر
            </a>
        </div>
    </div>
</main>